import Resources.DynamicArray;

public class Main {
    public static void main(String[] args) {
        DynamicArray newArray = new DynamicArray();
        newArray.createArray();
        newArray.setArraySize(2);
    }
}